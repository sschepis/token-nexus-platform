// parse-server/src/cloud/ai-assistant/types.js
// CommonJS version of AI Assistant types

/**
 * Represents the context of the user making the request.
 * This will be passed to tools for permission checking and context-aware actions.
 */

/**
 * Represents the input for an API request to the AI assistant.
 */

/**
 * Represents the response from the AI assistant.
 */

// Since this is CommonJS, we don't export interfaces but we can export factory functions
// or just use this file for documentation. The actual types will be handled at runtime.

module.exports = {
  // Export any utility functions if needed
};